machine:
    services:
        - docker

dependencies:
    override:
        - docker info
        # - docker build --rm=false -t adrianbrink/lol_store .

test:
    override:
        # - docker run adrianbrink/lol_store cargo test
        - docker-compose up cargo test